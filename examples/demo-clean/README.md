# Tracefuse demo fixture (clean)

Minimal Node project that should scan with a **high score** and no high/critical findings.

```bash
cargo run --release -- scan examples/demo-clean
# or
make scan-demo-clean
```

Compare with `examples/demo-vulnerable/` for the intentional mess.
