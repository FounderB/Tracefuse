// DEMO ONLY — intentional secret findings (FAKE/EXAMPLE). DO NOT USE.
const config = {
  // FAKE EXAMPLE Stripe-shaped key — not a real secret
  apiKey: "sk_live_FAKEEXAMPLEEMBEDDEDKEY999999",
  // FAKE EXAMPLE password assignment
  password: "FAKE_EXAMPLE_PASSWORD_embedded_demo",
  // FAKE EXAMPLE Slack-shaped token
  slackToken: "xoxb-FAKEEXAMPLE-9988776655-ZZZZYYYYXXXX",
};

console.log("demo-vulnerable fixture — Tracefuse demo only (all secrets FAKE/EXAMPLE)");
module.exports = config;
