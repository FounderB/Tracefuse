# DEMO ONLY — FAKE/EXAMPLE template that still embeds a realistic-looking key pattern.
# Tracefuse demos intentional "bad .env.example" hygiene. DO NOT USE these values.

# FAKE EXAMPLE — AWS access key id shape (not a real account)
AWS_ACCESS_KEY_ID=AKIAIOSFODNN7EXAMPLE

# FAKE EXAMPLE placeholders (empty or obviously fake)
AWS_SECRET_ACCESS_KEY=FAKEEXAMPLE/secret/key/do/not/use/abcdefghij
DATABASE_URL=postgres://demo:FAKE_EXAMPLE_PASS@localhost:5432/demo
